# SearXNG for Syraa's web_search tool. Config is baked in so Dokploy git deploys need no bind mounts.
# Pinned dated tag — bump deliberately (see https://hub.docker.com/r/searxng/searxng/tags).
FROM searxng/searxng:2026.9.22-2ed96e6fc
COPY settings.yml /etc/searxng/settings.yml
